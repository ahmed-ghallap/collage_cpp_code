#include <iostream>
#include <cstring>

using namespace std;

int main()
{
    // error 
    // strcat(name, "Ahemd");
    string name = "my name is ";
    string tmp = "1- " + name + "ahmed";
    cout << tmp << endl;
}